"""PDF export placeholder.

PDF generation is intentionally delayed until the Excel export and data schema
are stable. Streamlit Cloud PDF support depends on Linux fonts and rendering
libraries, so this module should stay isolated.
"""

